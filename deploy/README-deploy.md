
# Secure Deployment Guide
See Caddy and Nginx examples under `deploy/`. Systemd units target `/opt/crypto-bot`.
Steps: copy repo → create venv → install → set `.env` → enable systemd → configure proxy → open 80/443.
